// Service Worker pour installation Appli sur pc (Chrome)
//

self.addEventListener("install", event => {
    console.log("Service Worker installé");
});

self.addEventListener("fetch", event => {
});